import { mount } from '@vue-test-utils'
import cmp from '@/components/modal/Dialog.vue'

function buildWrapper() {
  return mount(cmp, {
    props: {

    }
  })
}


describe('Dialog.spec.js', () => {
  describe('slot: title', () => {
    
  });

  describe('slot: default', () => {
    
  });

  describe('slot: action', () => {
    
  });

  
});